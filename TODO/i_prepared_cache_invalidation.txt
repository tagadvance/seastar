CONCEPT: Invalidate cached prepared statements on UDT/type change
PRIORITY: 9 (edge-case correctness; only matters once ALTER TYPE exists)

SOURCE COMMENT (now removed):
- SeaStarCqlPrepareAsyncProcessor.java:59  handler to invalidate cached prepared statements on
  type changes; commented-out event-bus registration (// TODO)

CURRENT STATE:
SeaStarCqlPrepareAsyncProcessor caches prepared statements (Guava CacheBuilder.weakValues). The
real driver registers a TypeChangeEvent listener on the event bus to evict statements whose bind
variables reference a changed UDT. SeaStar logs the intent but registers nothing.

WHAT REAL DRIVER DOES:
On a TypeChangeEvent it re-prepares or evicts cached statements referencing the changed type
(see commented ctx.getEventBus().register(TypeChangeEvent.class, ...)).

IMPLEMENTATION PLAN:
1. Decide the eviction trigger. SeaStar has no Netty event bus; instead hook the point where a
   UDT is mutated (ALTER TYPE, see concept (b) mutability) to notify this processor.
2. On such a change, evict cached prepared statements whose variable definitions reference the
   changed type (or, simplest correct approach, clear the whole cache).
3. Only meaningful after UDT mutation (ALTER TYPE) exists; sequence after concept (b).

DEPENDENCIES: concept (b) UDT mutability; concept (a) variable definitions for targeted eviction.
